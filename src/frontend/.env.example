# Backend API base URL (no trailing slash)
# Development: local backend
# Production (separate host): https://api.example.com
# Docker image (same host as API): leave unset — production build uses same origin
VITE_API_BASE_URL=http://127.0.0.1:8000
